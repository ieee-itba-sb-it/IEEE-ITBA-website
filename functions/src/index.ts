// Firebase Cloud Functions have been removed/deprecated because Google Cloud / Cloud Functions is a paid service.
// Email newsletter features are handled directly on the client via SenderService in src/app/core/services/mailing/sender.service.ts.
